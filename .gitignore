# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/

# Database (stays local, never uploaded)
db/*.db
db/*.db-journal
db/*.db-wal
prisma/dev.db
prisma/dev.db-journal
prisma/dev.db-wal

# Download directory (generated files)
/download/

# Agent context (temp files)
/agent-ctx/

# Scripts output
/tmp/
# No subir archivos sin extensión ni logs
*.*
~/cryptoquant-terminal && rm -f \# Arranca el servidor && git add . && git commit -m limpieza: borrar logs subidos por error && git push.ts
~/cryptoquant-terminal && rm -f \# Arranca el servidor && git add . && git commit -m limpieza: borrar logs subidos por error && git push.tsx
~/cryptoquant-terminal && rm -f \# Arranca el servidor && git add . && git commit -m limpieza: borrar logs subidos por error && git push.js
~/cryptoquant-terminal && rm -f \# Arranca el servidor && git add . && git commit -m limpieza: borrar logs subidos por error && git push.jsx
~/cryptoquant-terminal && rm -f \# Arranca el servidor && git add . && git commit -m limpieza: borrar logs subidos por error && git push.json
~/cryptoquant-terminal && rm -f \# Arranca el servidor && git add . && git commit -m limpieza: borrar logs subidos por error && git push.css
~/cryptoquant-terminal && rm -f \# Arranca el servidor && git add . && git commit -m limpieza: borrar logs subidos por error && git push.md
~/cryptoquant-terminal && rm -f \# Arranca el servidor && git add . && git commit -m limpieza: borrar logs subidos por error && git push.svg
~/cryptoquant-terminal && rm -f \# Arranca el servidor && git add . && git commit -m limpieza: borrar logs subidos por error && git push.txt
~/cryptoquant-terminal && rm -f \# Arranca el servidor && git add . && git commit -m limpieza: borrar logs subidos por error && git push.sh
~/cryptoquant-terminal && rm -f \# Arranca el servidor && git add . && git commit -m limpieza: borrar logs subidos por error && git push.sql
~/cryptoquant-terminal && rm -f \# Arranca el servidor && git add . && git commit -m limpieza: borrar logs subidos por error && git push.toml
~/cryptoquant-terminal && rm -f \# Arranca el servidor && git add . && git commit -m limpieza: borrar logs subidos por error && git push.lock
